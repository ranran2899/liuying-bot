函数映射

- `impl.translate(text, target_language, llm_helper)` -> `run(text, target_language)`
- `impl.normalize_language(target)` 语言名归一化
- `impl.build_translate_tool(runtime)` -> `build_tools(runtime)`

暴露工具名：`translate_text`
