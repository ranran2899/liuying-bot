函数映射

- `impl.remember_fact(fact, tags, memory_manager)` -> 工具 `remember_fact`
- `impl.get_profile(memory_manager)` -> 工具 `get_user_profile`
- `impl.build_profile_tools(runtime)` -> `build_tools(runtime)`

暴露工具名：`remember_fact`（每会话配额 3 次）、`get_user_profile`
